This application contains common classes for interacting with Git repos, extracting values
from Lambda requests, encryption and more. It is shared by both the Jenkins and Github Actions
Workflow generators.